# Build trigger

This commit intentionally triggers the Windows Open Rails build workflow. The workflow checks out the Open Rails source, builds it on windows-latest, and packages the Northern Railway content/data.
